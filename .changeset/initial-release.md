---
'xrpl-message-verify': minor
---

Initial release: `verifyMessage`, `verifyMessageByAddress`, `verifySignInBlob`, `deriveAddress`,
`normalizeAddress`, the v1 challenge envelope (`formatChallenge`, `parseChallenge`,
`validateChallenge`) and `resolveAccount`.
