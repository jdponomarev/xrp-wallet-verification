{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'web: unit tests live in e2e (playwright)' && exit 0",
    "e2e": "playwright test"
  },
  "dependencies": {
    "xrpl-message-verify": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "5.9.3",
    "vite": "8.2.2"
  }
}
